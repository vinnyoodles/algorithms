{
  "name": "algorithms",
  "version": "1.0.0",
  "scripts": {
    "test": "jasmine",
    "coverage": "istanbul cover jasmine",
    "coveralls": "npm run coverage && cat coverage/lcov.info | coveralls"
  },
  "devDependencies": {
    "coveralls": "^2.11.15",
    "istanbul": "^0.4.5",
    "jasmine": "2.3.1",
    "underscore": "1.8.3"
  }
}
